{
  "github": {
    "release": true
  },
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}"
  },
  "npm": {
    "publish": true
  },
  "hooks": {
    "after:bump": "npm version ${version} --no-git-tag-version --prefix src/uview-ultra && git add src/uview-ultra/package.json && npm run build"
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  }
}
